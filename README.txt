Whatever you do, mention :

Plane.c = semaphore
Planes.c = mutex

The point of using argc/argv was for debugging purposes.

In order to run the program, just run either plane or planes after running the makefile using Linux.
This was compiled and tested using CCC servers.
